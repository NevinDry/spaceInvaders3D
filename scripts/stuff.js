function updateHighScore(){	$('.highscore').html("High Score :");	$.getJSON('./score/score.json', function(data) {		var scores = [];		$.each( data, function( key, val ) {			scores.push({name: key, score: val});		});				scores.sort(function (a, b) {			if (a.score > b.score)			  return -1;			if (a.score < b.score)			  return 1;			return 0;		});		console.log(scores);		for(var currentItem = 0; currentItem < 3; currentItem++){			$('.highscore').append( '</br>'+ scores[currentItem].name + '  :  ' + scores[currentItem].score + " points");		}		});  }	function show_prompt(score) {		$.ajax({			type: "GET",			url:"score/score.php",			data: "name=lol",			dataType: "html",			success: function(msg){				console.log("reussit");			}		});		/*$.getJSON('./score/score.json', function(data) {			$.each( data, function( key, val ) {				console.log(val);				console.log(score);				if(score > val){				     console.log("doom");					var name = prompt('New highscore ! Enter your name','yourname');					if (name != null && name != "") {						alert(name);						$.ajax({							type: "GET",							url:"score/score.php",							data: "name="+name,							success: function(msg){								console.log("reussit");								console.log(msg);							}						});					}				}					return false;			});		}); */    }